<li> <%= link_to category.name, '/t/' + category.permalink %>
   <% unless category.children.empty? %>
     <ul><%= render :partial => 'category', :collection => category.children %></ul>
   <% end %>
</li>

